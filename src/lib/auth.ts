// next-auth has been removed from this project.
// This stub exists to prevent import errors in any files
// that reference auth configuration.
export const authOptions = {};
export default authOptions;
